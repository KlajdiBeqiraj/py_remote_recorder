[tool.poetry]
name = "record-screen-apis"
version = "0.1.0"
description = ""
authors = ["KlajdiBeqiraj <k.beqiraj@myw.ai>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
